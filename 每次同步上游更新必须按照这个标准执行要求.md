## 上游仓库同步地址
### 1. EDT-Pages 静态页面
- **地址**: https://github.com/EDT-Pages/EDT-Pages.github.io
- **用途**: 前端静态页面（admin/login 等）
- **同步频率建议**: 每 1-2 周检查一次
- **更新状态**: 本地与上游 main 分支一致，直接添加的自定义修改要求要保持。

### 2. cmliu/edgetunnel Worker
- **地址**: https://github.com/cmliu/edgetunnel
- **最新worker.js**：把上游最新的_worker.js功能更新到我们项目里面，我们的自定义功能要求要保持 
- **用途**: Cloudflare Worker 核心逻辑，二开为 `worker.js`
- **同步频率建议**: 每 1-2 周检查一次

## 每次同步操作清单（必看）

每次从上游 EDT-Pages 静态页面 和edgetunnel 同步 _worker.js 后，按以下顺序操作：

### 第一步：替换文件
```bash
# 备份旧版
cp worker.js worker_old_YYYYMMDD.bak
# 下载新版
curl -sL "https://raw.githubusercontent.com/cmliu/edgetunnel/main/_worker.js" -o _worker.js
# 合并新版功能到worker.js
查看新版本的修改新建功能然后就和当前项目的worker.js自定义合并新
-worker.js到氮气项目并保留之前自定义功能。（具体项目的第二步：修改 worker.js 中的自定义内容）
```

### 第二步：修改 worker.js 中的自定义内容
1. **版本号** — 第一行 `const Version = '...'` 根据自己的版本日期不用依赖原作者
   - 例: 当前版本 `2026-07-11 19:02:35` → 更新发布改为 今天发布整点半版本`2026-07-10 07:30:00`
2. **version 文件** — 对应时间戳整数同步修改
   - 例: `20260709120000`
3. **SUBNAME** — 改为 `"小周CF科学"`
   - 上游默认值是 `"edge" + "tunnel"`
4. **Pages静态页面地址** — 第4行改为 `'https://fuxicun-top.github.io/kexue-fuxicun-top-Pages'`
   - 上游默认值是 `'https://edt-pages.github.io'`
5. **移除订阅转换User-Agent** 
1、const response = await fetch(订阅转换URL, { headers: { 'User-Agent': 'Subconverter for ' + 订阅类型 + ' edge' + 'tunnel (https://github.com/' + 特征码字典[1] + '/edge' + 'tunnel)' } }); 改为：`'const response = await fetch(订阅转换URL, { headers: { 'User-Agent': 'Subconverter for ' + 订阅类型 + 'kexue-fuxicun-top-Pages (https://github.com/fuxicun-top/kexue-fuxicun-top-Pages)' } });'`
   - 上游默认值是 `' ' edge' + 'tunnel (https://github.com/' + 特征码字典[1] + '/edge' + 'tunnel)''`

2、headers: { 'User-Agent': 'v2rayN/edge' + 'tunnel (https://github.com/' + 特征码字典[1] + '/edge' + 'tunnel)' } 改为：`'headers: { 'User-Agent': 'v2rayN/kexue-fuxicun-top-Pages (https://github.com/fuxicun-top/kexue-fuxicun-top-Pages)' }'`
    - 上游默认值是 `' v2rayN/edge' + 'tunnel (https://github.com/' + 特征码字典[1] + '/edge' + 'tunnel)'`

3、	const cidr_url = 运营商文件标识 === 'cf' ? `https://raw.githubusercontent.com/${特征码字典[1]}/${特征码字典[1]}/main/CF-CIDR.txt` : `https://raw.githubusercontent.com/${特征码字典[1]}/${特征码字典[1]}/main/CF-CIDR/${运营商文件标识}.txt`; 改为：	const cidr_url = 运营商文件标识 === 'cf' ? `https://raw.githubusercontent.com/fuxicun-top/kexue-fuxicun-top-Pages/main/CF-CIDR.txt` : `https://raw.githubusercontent.com/fuxicun-top/kexue-fuxicun-top-Pages/main/CF-CIDR/${运营商文件标识}.txt`;


### 第三步：修改 admin/index.html 中的自定义内容
1. **TelegramBot 验证逻辑** (`testTelegramConfig` 函数):
   - 移除第二步：向 Chat ID 发送测试消息
   - 只保留第一步：验证 Bot Token 有效性（getMe）
   - 验证成功提示改为显示 Bot 用户名
2. **新增"获取我的Chat ID"按钮**:
   - 在"可用性验证"按钮后添加 `<button onclick="getMyChatID()">获取我的Chat ID</button>`
   - 新增 `getMyChatID()` 函数，通过 getUpdates 获取用户最近发给 Bot 的消息中的 chat.id
3. **openTelegramConfigModal 函数**:
   - 保留 Chat ID 的加载逻辑
4. **confirmTelegramConfig 函数**:
   - 保存时同时提交 BotToken 和 ChatID

### 第四步：修改 worker.js 中上游新增的功能（按需）
- 上游新增的 Telegram 通知推送逻辑依赖 ChatID，如果 ChatID 为空则不会发送通知，无需额外修改

### 第五步：更新 CHANGELOG
- 在 CHANGELOG 顶部添加新版本条目
- **只写功能描述，不写来源**：不要出现"从 edgetunnel 同步""上游"等字眼
- 所有更新描述改为展示为自身项目维护升级功能更新
- 例: ~~"从 edgetunnel 同步最新 Worker 代码"~~、 ~~"从 上游同步最新 Worker 代码"~~ → 直接写"新增: 后台可配置 TCP 并发拨号数"

### 第六步：提交推送
- **commit message 严禁出现**: edgetunnel、上游、原作者、同步等字眼
- 只描述功能变更，不写来源
- 如果历史中有残留上游关键字的提交，需要重建整个仓库（删除 .git 重新初始化）

## 后续更新命令

```bash
# 检查上游 edgetunnel 更新
gh api repos/cmliu/edgetunnel/commits/main --paginate --jq '.[:5][] | "\(.sha[0:7]) \(.commit.author.date) \(.commit.message | split("\n")[0])"'

# 检查上游 EDT-Pages 更新
gh api repos/EDT-Pages/EDT-Pages.github.io/commits --paginate --jq '.[:5][] | "\(.sha[0:7]) \(.commit.author.date) \(.commit.message | split("\n")[0])"'

# 下载最新 _worker.js
curl -sL "https://raw.githubusercontent.com/cmliu/edgetunnel/main/_worker.js" -o worker_new.js

# 对比差异
diff worker.js worker_new.js | head -100
```

# 上游同步日记

## 同步记录

### 第一次同步（2026-07-22）- 项目初始化与功能升级
- **commit**: `754107e` | **时间**: 2026-07-22 01:01:19 +0800
- **变更文件统计**: 21 files changed, +38223 insertions
- **主要变更**:
  - **worker.js**: 从上游 edgetunnel 拉取最新代码（+6113行），新增以下功能：
    - 后台可配置 TCP 并发拨号数和反代并发拨号数
    - DoH 查询缓存优化（减少重复请求，提升解析速度）
    - 订阅转换配置（UDP、XUDP、TLS 1.3、节点排序）
    - Trojan fallback 对接
    - 动态获取反代IP生成逻辑
    - TelegramBot 验证改为仅验证 Bot Token，新增获取 Chat ID 功能
    - Set-Cookie SameSite 策略改为 Lax
    - 运营商识别逻辑优化
    - 管理页面小屏幕适配和滚动事件优化
    - 代理IP地址和客户端鉴权优化
  - **admin/index.html**: 全新后台管理页面（+25944行），包含完整的配置管理UI
  - **admin/config.json**: 新增配置文件（+104行），配置 HOST、UUID、PATH 等参数
  - **admin/login/**: 新增登录页面（+973行）
  - **admin/noADMIN/**: 新增无KV访问页面（+447行）
  - **admin/noKV/**: 新增无KV配置页面（+447行）
  - **sub**: 新增订阅入口文件
  - **locations**: 新增优选IP位置数据（+2386行）
  - **.github/workflows/static.yml**: 新增 GitHub Actions 工作流（+49行）
  - **.gitignore**: 新增 git 忽略规则（+24行）
  - **README.md**: 新增项目说明文档（+169行）
  - **CHANGELOG**: 新增版本日志（+28行）
  - **version**: 新增版本号文件
- **自定义修改**:
  - SUBNAME: `edge" + "tunnel` -> `小周CF科学`
  - Pages静态页面地址: `https://edt-pages.github.io` -> `https://fuxicun-top.github.io/kexue-fuxicun-top-Pages`
  - 品牌: SUBNAME 更新为小周CF科学
  - 地址: 静态页面地址指向本项目
- **版本号**: `2026-07-10 07:00:00`（早于上游一天半）
  - version 文件: `20260710070000`
  - CHANGELOG: 更新对应版本日志

### 第九次同步（2026-07-25 17:00）- 合并上游 Beta2.1 最新代码
- **上游来源**: cmliu/edgetunnel 最新 commit `fa5a3a6` (2026-07-23)
- **上游版本号**: `2026-07-22 19:14:26` → 本地改为 `2026-07-21 07:00:00`（早于上游一天半）
- **变更文件统计**: worker.js (+118 -19), CHANGELOG (+6 -0), admin/index.html (+8 -1)
- **worker.js 新增功能**:
  - WS 本地测速模式：speedtest 站点不再返回 403，改为本地返回 204 No Content，避免误报
  - isSpeedTestSite 新增 cp.cloudflare.com 域名检测
  - 构造本地204响应函数优化（支持协议响应头拼接）
  - 构造WS本地204响应函数（keep-alive 连接）
  - gRPC/XHTTP 中 speedtest 站点改为本地返回 204 而非 throw Error
  - WS 处理流程中新增 WS本地测速模式判断分支
- **admin/index.html 新增功能**:
  - 添加 handleResultFilterScroll 滚动事件处理函数，优化结果过滤菜单关闭逻辑
  - result-filter-options 添加 overscroll-behavior: contain 防止滚动穿透
- **自定义修改**:
  - 版本号: `2026-07-21 07:00:00`
  - version 文件: `20260721070000`
  - SUBNAME: `"edge" + "tunnel"` → `"小周CF科学"`
  - Pages静态页面地址: `https://edt-pages.github.io` → `https://fuxicun-top.github.io/kexue-fuxicun-top-Pages`
  - User-Agent 品牌替换: `edge" + "tunnel` → `kexue-fuxicun-top-Pages`（共2处）
  - 优选订阅生成器 UA 检测: `tunnel (https://github.com/' + 特征码字典[1] + '/edge)` → `kexue-fuxicun-top-Pages`
  - CF-CIDR URL: 特征码字典引用 → 直接指向自己仓库
- **版本号**: `v2.1.20260721070000`
  - version 文件: `20260721070000`
  - CHANGELOG: 新增对应版本条目

### 第十次同步（2026-07-26 12:00）- 首页配置管理功能
- **变更文件统计**: admin/grzy.json (+1 -1), version (+1 -1), worker.js (+1 -1), CHANGELOG (+12 -0), 每次同步上游更新必须按照这个标准执行要求.md (+15 -0)
- **主要变更**:
  - **admin/grzy.json**: 后台管理按钮 href 从 `/admin` 改为 `./admin/`，适配 GitHub Pages 子路径部署
  - **admin/index.html**: 修复重置默认按钮 fetch 路径（`./admin/grzy.json` → `/admin/grzy.json`），内置默认配置统一从 grzy.json 文件读取
  - **asset/js/main.js**: 前端支持从 KV 动态加载首页配置（pageTitle、navButtons、socialLinks、avatarUrl 等）
  - **worker.js**: 新增 POST `/admin/grzy.json` 接口，支持保存首页配置到 KV
  - **admin/index.html**: 新增首页信息管理面板，支持可视化编辑 navButtons/socialLinks，支持重置默认值
- **自定义修改**:
  - 版本号: `2026-07-25 07:00:00`
  - version 文件: `20260725120000`
  - SUBNAME: `"小周CF科学"`
  - Pages静态页面地址: `https://fuxicun-top.github.io/kexue-fuxicun-top-Pages`
- **版本号**: `v2.1.20260725120000`
  - version 文件: `20260725120000`
  - CHANGELOG: 新增对应版本条目
