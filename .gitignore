# VS Code 设置
.vscode/
demo/

# 操作系统
.DS_Store
Thumbs.db

# 日志文件
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 依赖目录
node_modules/
package-lock.json
yarn.lock

# 编辑器临时文件
*.swp
*.swo
*~
.idea/
.claude/settings.json

# 上游参考文件（不提交到仓库）
_worker_upstream.js

# 同步日记（本地参考，不提交）
每次同步上游更新必须按照这个标准执行要求.md

# 备份文件
worker_backup_*.js
每次同步上游更新必须按照这个标准执行要求.md
